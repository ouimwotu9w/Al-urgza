name: al_urjuza
description: "تطبيق الأرجوزة — أرجوزة الملل والنحل للشاعر عاصف بن الجمل، شرحاً وتفريعاً بتصميم Material 3."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  google_fonts: ^6.2.1
  share_plus: ^10.0.2
  screenshot: ^3.0.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
